function checkPalindrome(N, str) {
    let len =str.length;
    for(let i=0;i<len/2;i++)
    {
        if(str[i]!==str[len-1-i])
        {
            console.log("No");
            break;
        }
        else{
            console.log("Yes");
            break;
            
        }
    }
  
}
